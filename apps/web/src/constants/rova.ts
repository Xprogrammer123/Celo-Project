/** 1 real CELO buys this many ROVA (in-game credits). */
export const ROVA_PER_CELO = 100;

/** Cost to start one memory game round. */
export const ROVA_PER_GAME = 25;

/** Demo / guest starting balance (3 games). */
export const ROVA_DEMO_START = 75;

/** CELO sent when buying one ROVA pack. */
export const CELO_BUY_PACK = "1";

/** CELO cost for exactly one real game (25 ROVA at 100 ROVA / 1 CELO). */
export const CELO_PER_GAME = "0.25";
